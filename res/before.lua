
    function foo(a, b,
        c)
    end

    function foo(
        a, b)
    end

    a = {a,
    b}

    {
        a = foo(
        a, b, {
            1, 2, 3
        })
    }

    if (a == b
        and (c == d
        or c == e))
        then
            doit()
        end

    a, b, c =
    function_name(x,
    y, z)

    x = y +
    c +
    variable

    return { expr },
    err, err_mes

-- vim: foldlevel=10
